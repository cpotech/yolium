## What was implemented

- Restored project-row click-through for non-waiting status dots by only intercepting clicks when a waiting popover should open.
- Added stable sidebar test hooks (`data-status`, popover question/option test ids) for the new status-dot UI.
- Extended sidebar unit coverage for running/failed dot click-through.
- Added Playwright sidebar coverage for waiting-dot popover open, outside-click dismiss, Escape dismiss, and answer/resume flows.

## Files modified and why

- `src/renderer/components/navigation/StatusDotPopover.tsx`: fixed the click propagation regression and added stable attributes/test ids for the popover workflow.
- `src/tests/Sidebar.test.tsx`: added regression tests for clicking running/failed dots and updated popover assertions to use the new test hooks.
- `src/tests/e2e/helpers/selectors.ts`: added selectors for sidebar status dots and popover content.
- `src/tests/e2e/tests/kanban-sidebar.spec.ts`: added waiting-item sidebar setup helpers and end-to-end coverage for the new popover workflow.

## Tests added and results

- Added 2 Sidebar unit regressions for non-waiting dot click-through.
- Added 3 Playwright sidebar workflow tests for waiting-popover open/dismiss/answer flows.
- `npm test`: PASS — 1406 passing, 2 skipped.
- `npm run test:e2e -- src/tests/e2e/tests/kanban-sidebar.spec.ts`: PASS — 15 passing.
- `npm start`: build and Electron launch reached successfully in headless mode before DBus/X11 environment limits stopped the shell session.
- `npm run test:e2e`: still reproduces existing `src/tests/e2e/tests/app-launch.spec.ts` timeouts outside this sidebar change.

## Caveats or follow-up items

- The full Playwright suite is not green in this environment because the existing `app-launch.spec.ts` tests time out before reaching the rest of the suite.
- Vitest still emits pre-existing React `act(...)` warnings in unrelated tests.
