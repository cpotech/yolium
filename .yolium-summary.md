# Merge Conflict Resolution

## What was implemented

- Merged `origin/main` into `yolium-1773228512510-7d0cc1` and resolved the conflict in `ItemDetailDialog.tsx` by keeping the extracted shell architecture from the refactor branch.
- Carried forward the incoming detail-dialog worktree path display fix so long paths wrap cleanly and expose the full value with a `title` tooltip in the extracted sidebar.
- Brought in the upstream git-worktree module split, related IPC/test updates, and the status-bar branch/worktree tooltip improvement from `origin/main`.

## Files modified and why

- `src/renderer/components/kanban/ItemDetailDialog.tsx`: resolved the merge conflict in favor of the refactored shell so the dialog stays modular after merging `origin/main`.
- `src/renderer/components/kanban/item-detail/ItemDetailSidebar.tsx`: applied the upstream worktree-path rendering fix inside the extracted sidebar component.
- `src/main/git/git-branch-operations.ts`: added via the upstream git-worktree refactor merge.
- `src/main/git/git-github-pr.ts`: added via the upstream git-worktree refactor merge.
- `src/main/git/git-internal.ts`: added via the upstream git-worktree refactor merge.
- `src/main/git/git-repository.ts`: added via the upstream git-worktree refactor merge.
- `src/main/git/git-worktree-lifecycle.ts`: added via the upstream git-worktree refactor merge.
- `src/main/git/git-worktree-paths.ts`: added via the upstream git-worktree refactor merge.
- `src/main/git/git-worktree.ts`: updated via the upstream git-worktree refactor merge.
- `src/renderer/components/StatusBar.tsx`: updated via the upstream worktree/branch tooltip improvement merge.
- `src/tests/git-branch-operations.test.ts`: added via the upstream git-worktree refactor merge.
- `src/tests/git-github-pr.test.ts`: added via the upstream git-worktree refactor merge.
- `src/tests/git-rebase-ipc.test.ts`: updated via the upstream git-worktree refactor merge.
- `src/tests/git-repository.test.ts`: added via the upstream git-worktree refactor merge.
- `src/tests/git-worktree-lifecycle.test.ts`: added via the upstream git-worktree refactor merge.
- `src/tests/git-diff.test.ts`: removed by the upstream git-worktree refactor merge.
- `src/tests/git-worktree.test.ts`: removed by the upstream git-worktree refactor merge.
- `.yolium-summary.md`: rewritten with the final post-merge summary required by Yolium.

## Tests added and results

- Ran `npm test`
  - Result: 84 files passed; 1378 tests passed, 2 skipped.
- Ran `npm run test:e2e`
  - Result: 110 tests passed, 9 skipped.
- HTML reports generated:
  - `vitest-report/index.html`
  - `playwright-report/index.html`

## Caveats or follow-up items

- The unit suite still emits pre-existing React `act(...)` warnings in several UI tests, but they are warnings only and the run completed successfully.
- The renderer build still emits the existing large-chunk warning during `npm run test:e2e`.
- The earlier local pre-merge artifacts were stashed under `yolium-merge-conflict-prep` before merging `origin/main`.
